import React, { Component } from 'react';
import { Container } from 'react-bootstrap';

export default class UpdateArticle extends Component {
    render(){
        return(
            <Container >
                <h2>Update Article Page</h2>
            </Container>
        )
    }
}