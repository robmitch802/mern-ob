import React, { Component } from 'react';
import { Container } from 'react-bootstrap';

export default class CreateMeeting extends Component {
    render(){
        return(
            <Container >
                <h2>Create Meeting Page</h2>
            </Container>
        )
    }
}